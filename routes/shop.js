const express = require('express');
const router = express.Router();

// Define your shop routes here
router.get('/products', (req, res, next) => {
    // Handle /shop/products route
});

module.exports = router;
